#!/usr/bin/env python3
"""Checkout shim: the real CLI is atriumd/cli.py (also the `atrium` entry
point of the installed package)."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "atriumd"))
from cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
