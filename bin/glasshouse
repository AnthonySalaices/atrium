#!/usr/bin/env python3
"""Checkout shim: the real CLI is glassd/cli.py (also the `glasshouse` entry
point of the installed package)."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "glassd"))
from cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
