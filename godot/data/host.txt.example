# The address of the machine running atriumd, as the headset sees it.
# Copy this file to host.txt and put your own address in it — host.txt is
# gitignored, like token.txt, so nobody publishes their LAN layout by accident.
192.168.1.50
